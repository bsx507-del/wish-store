/** @type {import('next').NextConfig} */
const nextConfig = {
  poweredByHeader: false,
  output: "export",
  trailingSlash: true,
  basePath: "/wish-store",
  assetPrefix: "/wish-store/",
};

export default nextConfig;
