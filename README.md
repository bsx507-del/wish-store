# وِشّ — متجر اللحظات الرقمية

واجهة عربية RTL لمتجر ترفيهي يبيع لحظات وهدايا رقمية بأسعار رمزية. النسخة الحالية تعرض تجربة الدفع وتسجيل البريد محليًا، بينما ربط الدفع الحقيقي يحتاج حسابًا لدى Stripe أو Moyasar ومفاتيح سرية على الخادم.

## التشغيل المحلي

يتطلب Node.js 18.17 أو أحدث:

```bash
npm install
npm run dev
```

ثم افتح `http://localhost:3000`.

## النطاق

- صفحة هبوط عربية متجاوبة ومناسبة لحركة المرور من TikTok.
- كتالوج محلي مصنف مع بحث وتصفية، يضم ١٢٧٣ منتجًا موزعًا على الأقسام مع تحميل تدريجي للأداء.
- سلة افتراضية وحساب الإجمالي بالهللات والريال.
- حساب بالبريد الإلكتروني فقط محفوظ محليًا في نسخة الـMVP.
- قسم نموذج العمل ومسارات الدخل: المنتجات الرقمية، العضوية، والهدايا.
- مكافأة يومية اختيارية بدون إعلانات مزعجة.
- المكافأة اليومية تضيف ٢٥ هللة فعلية إلى الرصيد التجريبي وتُقفل حتى اليوم التالي.
- خلفية تفاعلية هادئة تتبع مؤشر الماوس مع وضع ثابت تلقائيًا للأجهزة اللمسية وتقليل الحركة.
- محفظة تجريبية محفوظة محليًا مع شحن رمزي وتحقق من الرصيد قبل إتمام الطلب.
- سجل طلبات محلي يعرض رقم الطلب والتاريخ والمنتجات والإجمالي بعد كل عملية محاكاة.
- مفضلة محلية ومشاركة المنتجات، مع أسئلة شائعة وشروط وخصوصية داخل الواجهة.
- فرز المنتجات حسب السعر أو الاسم، مع فلتر سريع للمفضلة.
- بحث عربي مرن يتجاهل اختلافات الهمزات والتاء المربوطة، مع حالة فارغة قابلة لإعادة الضبط.
- زر تفريغ للسلة وتحسينات إضافية لتجربة الجوال.
- روابط المشاركة تفتح تفاصيل المنتج مباشرة، مع اقتراحات مشابهة داخل نافذة المنتج.
- حماية من النقر المكرر أثناء حفظ الطلب التجريبي.
- نافذة المنتج تنعكس في رابط الصفحة، لتعمل روابط المشاركة والرجوع دون فقدان المنتج المختار.
- البحث والتصنيف والفرز والمفضلة تنعكس أيضًا في الرابط، لتسهيل مشاركة قائمة مختارة.
- مخطط Supabase مبدئي في `supabase/schema.sql` وملف إعدادات آمن في `.env.example` للانتقال إلى الحسابات والطلبات الحقيقية.
- مسار الانتقال التجاري موثق في `docs/production-launch.md`، مع adapter خادمي لموياسر في `server/moyasar.ts`.
- إعدادات المسارات تتكيف تلقائيًا بين GitHub Pages (`/wish-store`) وVercel (المسار الجذري) لتجنب صفحة 404 بعد النشر.
- زر Google أصبح مربوطًا بـ Supabase Auth عند توفر متغيرات البيئة، مع استمرار خيار البريد المحلي في النسخة التجريبية.
- أضيفت دالة Vercel `api/payments/create.ts` للتحقق من جلسة المستخدم والأسعار من Supabase وإنشاء طلب Moyasar بحالة `pending` قبل بدء الدفع.
- أضيف webhook للتحقق من حالة الدفع مباشرة من Moyasar قبل تحويل الطلب إلى `paid` أو `failed`.
- يتم تنظيف السلة والمفضلة القديمة تلقائيًا عند فتح الموقع حتى لا تتسبب بيانات نسخة سابقة في إجماليات أو عناصر غير صحيحة.
- أضيفت manifest وrobots وsitemap وواجهة 404 لتحسين التثبيت والفهرسة والتعامل مع الروابط غير الصحيحة.
- لا يوجد شحن أو تسليم؛ كل المنتجات والطلبات افتراضية للترفيه فقط.

## ملاحظة الدفع وقاعدة البيانات

النسخة المنشورة حاليًا تعمل كـ static export على GitHub Pages، لذلك لا يمكنها حفظ بيانات حساسة أو تنفيذ دفع حقيقي من المتصفح. تم تجهيز مخطط Supabase ومتغيرات البيئة كبداية للنسخة الإنتاجية؛ عند نقل التطبيق إلى استضافة خادمية مثل Vercel يجب إضافة مسار خادمي لإنشاء جلسة Moyasar والتحقق من webhook، ثم نقل الطلبات والمستخدمين من التخزين المحلي. لا تضع `SUPABASE_SERVICE_ROLE_KEY` أو مفتاح Moyasar السري في `NEXT_PUBLIC_*` أو داخل المستودع.

## كيف يتحول إلى منتج مدفوع؟

1. اختر مزود الدفع: Stripe للبطاقات العالمية أو Moyasar للسعودية.
2. أضف API route على الخادم لإنشاء Checkout Session، ولا تضع مفاتيح الدفع في الواجهة.
3. استبدل زر «متابعة للدفع عند الإطلاق» برابط جلسة الدفع.
4. استخدم Supabase Auth أو Clerk لتسجيل البريد الحقيقي بدل التخزين المحلي.

## قائمة ما قبل الإطلاق الفعلي

- [x] نشر نسخة تجريبية عامة على GitHub Pages.
- [x] فحص البناء والنشر تلقائيًا عبر GitHub Actions.
- [x] فهرسة أساسية وmanifest وواجهة 404.
- [ ] إنشاء مشروع Supabase وتشغيل `supabase/schema.sql`.
- [ ] نقل التطبيق إلى استضافة خادمية مثل Vercel لإضافة API routes.
- [ ] إضافة مصادقة حقيقية، ثم نقل الطلبات والمحفظة من `localStorage`.
- [ ] فتح حساب Moyasar/Stripe في وضع الاختبار، وإضافة مفاتيح server-only في إعدادات الاستضافة.
- [ ] بناء webhook يتحقق من نتيجة الدفع قبل تسجيل الطلب.
- [ ] مراجعة سياسة الخصوصية والشروط مع مختص قبل تحصيل أي مبلغ.
- [ ] اختبار الدفع في sandbox من الجوال والكمبيوتر قبل التحويل إلى live.
